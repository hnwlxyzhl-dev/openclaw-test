# 2026-06-30 每日笔记

## 宏观经济推送程序 - 进出口分行业数据增强

### 背景
老板要求在 `/home/admin/Hualin/MacroEconomics_push.py` 中新增「分行业（73类HS商品）进出口同比增速折线图」功能。

### 今日完成

#### 1. 贸易差额单位Bug修复 ✅
- **问题**：第2048行 `(df['出口额'] - df['进口额']) / 1e6` 除数错了10倍
- **AkShare数据单位是千美元**，换算亿美元应 ÷1e5，不是 ÷1e6
- **影响**：贸易差额图表数值偏小10倍（如显示105亿美元，实际应为1054亿美元）
- **修复**：已改为 `/ 1e5`
- **验证**：用2026年5月实际数据（海关总署/财新）交叉验证

#### 2. 备份 ✅
- 备份文件：`MacroEconomics_push.py.bak.20260630_224000`（168KB）

#### 3. 分行业进出口数据源调研 ✅
调研结论：
| 数据源 | 内容 | 历史 | 可用性 |
|--------|------|------|--------|
| AkShare `hgjck` | 总量，无细分 | 2008至今 | ✅ 已用 |
| 商务部 composition 接口 | 73个HS分类 | **仅最新一期** | ✅ 可用 |
| 商务部 totalmonth | 总量月度 | 仅4个月 | ⚠️ 有限 |
| 商务部 jdmonth 等 | 4大类(机电/高新/服装/农产品) | 停在2020年 | ❌ 已废弃 |
| 海关总署网站 | 完整数据 | 有历史 | ❌ WAF封IP(412) |
| 海关广东分署 xls 文件 | 完整HS分类 | 有历史 | ✅ **文件可直接下载**（页面被封但xls URL可提取） |
| CnOpenData/RESSET/Wind | 完整 | 有历史 | ❌ 付费 |

#### 4. 历史数据缓存构建 ✅
- **方案**：tavily搜索 → 找到海关广东分署构成表页面URL → tavily extract提取xls链接 → 下载Excel解析
- **缓存文件**：`/home/admin/Hualin/data/trade_composition_cache.json`
- **已获取9个月数据**：202403, 202404, 202405, 202406, 202409, 202506, 202509, 202510, 202604
- 每月73个HS分类商品，含当月值、累计值、累计同比
- **注意**：部分月份因tavily配额限制未能获取，后续可补

#### 5. 代码框架（子代理生成）✅
新增函数：
- `_load_trade_comp_cache()` / `_save_trade_comp_cache()` — 缓存读写
- `_fetch_mofcom_composition()` — 商务部API获取最新一期
- `_fetch_mofcom_composition_all_pages()` — 分页获取
- `_parse_trade_composition_from_text()` — 新闻文章解析
- `_fetch_trade_history_from_news()` — 新闻爬取历史（弱，需改进）
- `fetch_trade_composition_multi()` — 多源合并
- `plot_trade_composition_detail()` — 分3组子图绘图
- 已集成到 `plot_china_trade()` 末尾，紧跟总量图后推送

### 明日待完成

#### 🔴 高优先级
1. **修改数据获取逻辑**：`fetch_trade_composition_multi()` 要改为「读缓存 + 商务部API追加最新」模式
   - 缓存已有9个月数据，每次运行只需从商务部获取最新一期追加
   - 新闻爬取逻辑（`_fetch_trade_history_from_news`）太弱，需要重写或移除
   
2. **绘图数据指标问题**：当前缓存中的 `export_lj_per` / `import_lj_per` 是**累计同比**（从年初到当月的累计）
   - 老板要的是**单月同比**
   - 方案：用相邻月份的累计值反推单月同比（和工业利润一样的反推法）
   - 如果月份间距太大（如202506→202509），反推不准确，需要标注

3. **测试运行**：完整运行 `plot_china_trade()` 验证绘图效果
   - 确认子图分组正确
   - 确认推送顺序正确（总量图 → 分行业子图，不拆开）

4. **补充历史数据**：用tavily搜索更多月份的xls文件
   - 缺失月份：202407, 202408, 202410, 202411, 202412, 202501, 202503, 202504, 202505, 202508, 202511, 202512, 202601-202603
   - 注意tavily有每日配额限制

#### 🟡 中优先级
5. 海关广东分署的URL模式：部分URL是 `gdfs.customs.gov.cn`，部分是 `www.customs.gov.cn`
   - 两者的页面结构可能不同
   - www.customs.gov.cn 的页面被封但xls可能也能直接下载
6. 绘图标题需要标注「累计同比」还是「单月同比」

### 关键技术细节
- 海关广东分署xls文件可直接下载（页面412但文件200）
- tavily extract API key: `tvly-dev-DkxZg...`（存在 ~/.openclaw/.env）
- 商务部 composition 接口：`POST https://data.mofcom.gov.cn/datamofcom/front/composition/query`
  - 参数 `date=YYYYMM` 无效，只返回最新一期
- 海关广东分署构成表URL模式：
  - 目录: `http://gdfs.customs.gov.cn/customs/302249/zfxxgk/2799825/302274/302277/302276/{ID}/index.html`
  - xls: 同目录下 `/{timestamp}.xls`
  - Excel格式: 第2列=商品名, 第3列=出口当月, 第4列=出口累计, 第5列=进口当月, 第6列=进口累计, 第7列=出口累计同比, 第8列=进口累计同比

### 文件清单
- 主程序: `/home/admin/Hualin/MacroEconomics_push.py`（191KB，含新增代码）
- 备份: `/home/admin/Hualin/MacroEconomics_push.py.bak.20260630_224000`
- 缓存: `/home/admin/Hualin/data/trade_composition_cache.json`（9个月数据）

### 用户确认的需求
1. ✅ 73个HS分类商品（不需要更细或更粗）
2. ✅ 按出口同比增速排序分3组（高增长/普通/低增长），参考工业利润的子图方式
3. ✅ 每个子图画两条线：出口同比 + 进口同比
4. ✅ 紧贴总量图后推送，不拆开
5. ✅ 从2022年起（当前数据从2024年3月起，后续逐步积累）
6. ✅ 历史数据缓存 + 每次运行实时获取最新
