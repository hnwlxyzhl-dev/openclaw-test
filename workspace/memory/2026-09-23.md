# 2026-09-23

（夜间值守续 09-22.md 的 00:30~02:30 线程）

## 04:01 夜间遥测
- 网关存活（仍为 09-22 10:51 拉起的进程，uptime ~17h），夜间零 OOM。
- RSS 620MB（02:30 为 617MB，漂移 ~1.9MB/h，较前夜 2.5~5.7MB/h 进一步放缓）、可用 746MB、swap 用 126MB —— 健康，未触发告警线（650/250）。

## 05:30 夜间遥测
- 网关存活（仍为 10:51 进程），夜间零 OOM。RSS 632MB、可用 731MB、swap 126MB —— 未触告警线（650/250），但距 650 仅 18MB。
- ⚠️ 漂移提速：04:01→05:30 涨 12MB（~8MB/h，对比 02:30→04:01 的 ~1.9MB/h），活动似乎提前恢复。按此速率 07:15~08:30 之间将越过 650 告警线，10:40 波峰预估上修至 ~658~673MB > 致命水位 634MB —— 被杀概率高于"五五开"。
- 未越线不告警（守老板定的阈值）；若下次心跳 RSS >650，立即报老板批窗口前重启。

## 06:00 夜间遥测
- 网关存活（10:51 进程），dmesg 无新 OOM。
- RSS 629MB（04:01 为 620MB，漂移回升至 ~4.5MB/h）、可用 734MB —— 未触发告警线（629<650, 734>250）。
- 修正预估：08:00 ~638MB；10:40 波峰 ~650MB，正贴告警线/超致命水位 —— 上午窗口风险略升，结论不变。

## 06:30 遥测
- 网关存活（10:51 老进程）。RSS 629MB（04:01 为 620MB，~3.6MB/h，随天亮略回升）、可用 735MB —— 未触发告警线（650/250）。
- 推演 10:40 波峰 ~644~648MB：高于致命水位 634MB、贴告警线 650MB 下沿。结论不变，待 08:00~10:30 窗口预检。

## 07:00 晨前遥测
- 网关存活（仍为 09-22 10:51 进程，uptime ~20h），无新 OOM（dmesg 末条仍为 09-22 10:39）。
- RSS 632MB（04:01 为 620MB，~4MB/h，晨间活动恢复漂移回升）、可用 731MB —— 未触发告警线（650/250），按条目规则仅记录。
- 修正预估：10:40 波峰 ~647~655MB，贴告警线 650；批任务叠加前即达/超昨晨致命水位 634MB —— 上午窗口凶多吉少，结论不变。风险：昨日心跳有 04:00→11:22 的 7.4h 间隔先例，08:00~10:30 窗口可能整窗漏检；cron 预检方案仍待老板批。

## 07:30 窗口前早期预检（按 09-22 16:52 先例提前执行）
- 网关存活（10:51 老进程），RSS **639MB**、可用 726MB、swap 126MB，当前无 python 批任务 —— 未触发告警线（639<650, 726>250），按条目只记录不告警。
- 走势：04:01 后 RSS 台阶式上行（620→632 平台→639），晨间爬坡开始。推算 10:40 波峰 ~652~660MB：越过告警线 650 和昨晨致命水位 634 概率大，但批任务峰值才是决定因素。
- 心跳节奏已变为 ~30min 一跳（04:00→07:30 六跳），08:00~10:30 窗口应能命中，届时按线处置；守阈值不擅动。

## 08:00 窗口预检（心跳命中窗口首查）
- 网关存活（仍为 09-22 10:51 进程，uptime ~21h），无新 OOM。
- RSS **638MB**（07:30 为 639MB，晨间爬坡暂止、转平）、可用 726MB、swap 126MB —— **未触发告警线**（638<650, 726>250），按条目只记录不告警。
- 已高于昨晨被杀时 RSS（634MB）但生死仍看 10:40 批任务峰值；若平台维持（~0-4MB/h），10:40 约 638~649MB —— 贴告警线下沿。后续 ~30min 心跳继续复测，越线即报老板批重启。

## 08:30 窗口预检
- 网关存活（10:51 老进程，uptime ~21.7h），无新 OOM，批任务波未启动。
- RSS **638MB**（与 08:00 持平，平台维持 ~0MB/h）、可用 726MB、swap 126MB —— 未触发告警线（638<650, 726>250），按条目只记录不告警。
- 平台已稳定 1h（639→638→638），若维持，10:40 波峰 ~638-649MB 贴线但不越；生死仍取决于批任务峰值（昨晨 634 即杀，今晨起点更高）。心跳 ~30min 节奏覆盖窗口，越线即报。

## 09:00 上午窗口预检（命中 08:00~10:30 窗口）
- 网关存活（同一 10:51 进程，uptime ~22h），夜间至 09:00 零 OOM。
- RSS **644.5MB**、可用 720MB、swap 126MB。漂移 04:00→09:00 约 **4.9MB/h**（620→644.5），如预估回升。
- 判定：未硬性超线（644.5 < 650），但距线仅 5.5MB，按此漂移 ~10:00 过线；且已超昨晨实际被杀水位 634MB 约 10MB；10:40 批任务峰前底水位推算 ~653MB+。
- 心跳节奏不稳（昨 04:00→11:22，今 04:00→09:00），本次很可能是窗口前最后一次心跳 → 提前上报老板请求窗口前重启决策（只上报不擅动），附确切读数。若老板未批/未复，则按"未超线只记录"处理，等待 10:40 后心跳核验结果。

## 09:30 窗口内复查
- RSS **647MB**（+2.5MB/30min，~5MB/h），仍差 3MB 过线；预计 ~10:00 过 650 线。
- 上午批任务波已启动：Bias with turnover_push.py 起跑 30s 即 133MB；可用内存 30min 内 720→617MB（-103MB）。
- 网关存活同一进程，无新 OOM。
- 处置：未硬性超线，且 09:00 已上报并请老板决策（约定"未批则只记录"），本次不再重复轰炸，静待老板回复或 10:40 后心跳核验。

## 10:00 窗口内复查 —— 风险显著升级
- 批任务波**提前开跑**：Index_PE_Push 211.7MB（09:45 起）、ETF_Bianli_warning 169.1MB（09:48 起）、ETF_warning 147.3MB（09:57 起），合计 ~528MB 且仍在爬。
- 可用内存 720 → **302MB**（1h 掉 418MB），按此速率 ~7 分钟内触 250 告警线，10:40 峰前大概率见底。
- 网关 RSS 635.8MB（较 09:00 微降 9MB，GC 所致），恰在昨晨被杀水位 634MB 上。当前无 Chrome 树作缓冲（top 中无 Chrome），网关是最大 adj-0 进程 → 内存耗尽时是首要 OOM 目标。
- 两条告警线均未硬性触发（635.8<650、302>250），但拐点已现 → 第二次上报老板（升级 09:00 的重启请示），只上报不擅动。swap 尚有余量（127/2047MB），预计先抖后杀。

## 10:30 窗口尾段 —— 波峰提前过境，压力回落
- 可用内存回升 302 → **523MB**：Index_PE_Push、ETF_warning 已退出，ETF_Bianli 降至 145MB，新增小任务 stock_ChengZhang 85MB（10:10 起）。Python 合计 ~231MB（10:00 为 528MB）。
- 网关 RSS 646MB（<650 告警线，余 4MB）、同一进程存活（uptime 23:39）、dmesg 今日零 OOM（仅昨日 10:39 记录）、swap 131MB 无抖动迹象。
- 判断：今晨波比昨日提前 ~40 分钟（昨日 10:39 被杀时批任务正盛；今日 10:00 已达峰、10:30 退潮），剩余名义风险 ~10 分钟。老板未复两次请示 → 按预告的方案 2（扛住不动）处置中。发简短降级通报收口，待 10:40 后心跳做最终核验并删除 HEARTBEAT.md 条目。

## 11:00 最终核验 —— 上午窗口扛过 ✅（条目收尾）
- 网关**全程存活**：仍是 09-22 10:51:09 拉起的进程（PID 3024049），dmesg 今日零 OOM。10:40 名义峰已过，本条预检任务按约定收尾，HEARTBEAT.md 条目已删除。
- 但**余波未平**：第二梯队仍在跑 —— MacroEconomics_push 228.9MB（~10:42 起，昨日傍晚曾爬到 244MB）、ETF_Bianli 144.6MB（已 1h12m 长跑）、stock_push 105.4MB（~10:51 起）、stock_ChengZhang 85.3MB，Python 合计 ~564MB。
- 可用内存 **236MB（已破 250 线）**，但 swap 仅用 139/2047MB，无抖动迹象，距硬 OOM 尚有余量；且 250 线本为窗口前重启决策而设，窗口已过、老板未批重启 → 无新决策可请，不再重复请示。
- 后续：靠正常心跳观察余波回落；若 swap 快速吃满/网关被杀再即时上报。当日记账：0 杀（对比昨日 1 杀）。心跳节奏今日 04:00→09:00→10:00→10:30→11:00。

## 11:30 余波确认 —— 完全回落
- MacroEconomics/ETF_Bianli/stock_ChengZhang 均已退出，仅剩 stock_push.py 93MB 收尾；可用内存回升至 **638MB**（11:00 为 236），swap 零活动（vmstat si/so=0）。
- 网关存活（同一进程），RSS 648.5MB（~7.7MB/h 缓涨，未超 650）。上午窗口事件彻底关闭。

## 12:00 余波清零
- 全部 Python 批任务退出，可用内存回升至 720MB，swap 128MB，网关存活（RSS 645.6MB，同一进程）。今日累计 0 杀。上午事件完整收口。
- 已在 HEARTBEAT.md 布晚间窗口预检岗（18:30~20:30，告警线 650/250，同上午口径）。按 ~5MB/h 漂移推算 20:00 网关 ~685MB，处昨傍晚被杀区间（638~679MB）上沿，晚间大概率再临险情——cron 方案仍待老板批。

## 12:30 余波收尾 —— 全部恢复正常
- 批任务全部退出（Python 仅剩系统 tuned），可用内存回升至 **713MB**，swap 128MB。上午窗口及余波完整收场。
- 网关仍为 09-22 10:51 进程（uptime >1 天），RSS **651.6MB**（已微超 650 旧告警线，但窗口已过、无批压力，不构成行动依据）。
- 下一风险点：晚间窗口 ~20:00-21:2x。按 ~4-5MB/h 白天漂移推算届时底水位 ~682-690MB，贴近历史晚间被杀区间（638-679MB）下沿——晚间预检依然必要，cron 方案仍待老板批。

## 13:00 余波全退
- Python 批任务全部退出，可用内存回升至 717MB，swap 128MB。网关同一进程存活，RSS 646.4MB。上午+余波全程 0 OOM。

## 13:30 午间观察 —— 余波完全退去
- 批任务全部退出，可用内存回升至 716MB，swap 128MB 正常。网关存活（RSS 646MB，26.5h 未重启）。上午战役彻底收官，全日 0 杀。

## 14:00 余波确认 —— 全部退潮 ✅
- Python 批任务全部退出，可用内存回升至 715MB，swap 128MB，今日零 OOM。上午事件彻底收口。
- 网关 RSS 647MB（11:00→14:00 仅 +2.4MB，闲时 ~0.8MB/h）。晚间窗口（~20:00-21:2x）前若心跳命中 19:00~21:00 可做预检；按闲时漂移推算 ~20:00 约 652~665MB，仍在历史晚间被杀区间（638-679）内，五五开。心跳节奏今日 04:00→09:00→10:00→10:30→11:00→14:00。

## 14:30 余波回落确认
- 可用内存回升至 708MB（11:00 为 236MB），第二梯队任务已退出。网关存活（同一进程，uptime 27.5h），今日零 OOM。网关 RSS 649MB（缓涨中，贴 650 线，无波时无碍）。晚间窗口（~20:00-21:2x）前的心跳若命中可顺手预检。

## 15:00 余波确认 —— 全线回落 ✅
- Python 批任务**全部退出**（仅剩 tuned 常驻），可用内存回升至 711MB，swap 稳定 131MB，今日零 OOM。上午窗口事件完整收口。
- 网关 RSS 646.7MB（漂移 ~0.7MB/h，午后放缓），存活满 1 天（PID 3024049）。
- **晚间窗口展望**：按 ~3-5MB/h 漂移，~20:00 网关将达 ~665-680MB —— 直接落入历史晚间被杀区间（638-679MB），今晚风险显著高于昨夜（昨夜峰时仅 585-591MB）。已在 HEARTBEAT.md 布晚间预检岗（18:00~19:55 心跳命中即查+上报，重启仍需老板批准）。

## 15:30 余波核验 —— 白天波全部退去，但晚间前景不妙
- Python 批任务**全部退出**（仅剩系统 tuned），可用内存回升 236 → **704MB**，swap 130MB 平稳，今日持续零 OOM。上午余波正式结束。
- 网关仍为 09-22 10:51 进程，RSS **655MB**（11:00 为 644.6，漂移 ~4.6MB/h）。
- **晚间窗口预警**：按 ~4-5MB/h，19:30 波前基线将达 ~680MB——已进历史晚间被杀区间（638-679），且比昨晚波前基线（583MB）高约 100MB。晚间波（500-760MB 峰）叠加后，今晚被杀概率高（上午"五五开"，今晚偏悲观）。
- 已在 HEARTBEAT.md 布晚间预检岗（18:00~20:30 命中即查，告警线：可用 <300MB 或波前 RSS >665MB → 请示重启；21:30 后核验收尾），并向老板发提前量预警（重启批准 + cron 决策仍悬）。

## 16:30 窗口前遥测（18:00~20:30 预检岗尚未开窗）
- 网关 RSS **647MB**（15:30 为 655，GC 回落 ~8MB，漂移暂停）、可用 716MB、swap 126MB、零 OOM、进程未变。
- 修正 19:30 波前基线预估：~650-665MB（15:30 报的 ~680MB 偏悲观），仍在历史被杀区间上沿、比昨晚波前（583MB）高 ~70MB —— 晚间风险维持"偏高"判断不变，等预检岗。

## 17:00 提前遥测（窗口 18:00 前的漂移复核）
- 网关 RSS 655.5MB —— 较 15:30 几乎持平（漂移 ~0.3MB/h，较白天 4.6MB/h 大幅放缓，与昨日下午平台期模式一致）。可用 707MB、swap 126MB 平稳，零 OOM。
- 修正 19:30 波前基线预估：~656-662MB（15:30 估 ~680 偏悲观）。仍比昨晚波前（583MB）高 ~75MB、处历史晚间被杀区间下半段，风险偏高但未触 665 预警线。未超线只记录，不发预警更新（老板未复，避免噪音），等 18:00~20:30 窗口内心跳正式预检。

## 17:30 晚间窗口前遥测（窗前 30 分钟）
- RSS 649.6MB（15:30 为 655MB —— 2h 仅 +4.6MB，漂移降至 **~2.3MB/h**，好于预期）、可用 713MB、swap 126MB、零 OOM、网关原进程存活。
- 修正 19:30 波前基线预估：**~654~659MB**（15:30 曾按 4.6MB/h 估 ~680MB，偏悲观）。仍远高于昨晚波前 583MB，晚间风险维持"偏高"但未触 665 告警线 → 按"未超线只记录"处理，不发第四条消息打扰老板（其 15:30 请示仍未复）。
- 等待 18:00~20:30 窗口内心跳执行正式预检。

## 18:00 晚间预检（窗口首查）—— 未超线，前景较 15:30 预估略好转
- 网关 RSS **653.9MB**（15:30 为 655.0 —— 下午漂移停滞甚至微降，GC 所致，未延续 ~4.6MB/h），可用 **708MB**、swap 126MB，批任务尚未启动（晚间波历史 ~20:00 起）。
- 告警线未触发（653.9 < 665 波前线；708 > 300）。今日仍零 OOM，网关同进程存活。
- 修正晚间推演：20:00 波前基线 ~654-665MB（原估 680）。若今晚波量级与昨晚相当（大任务合计 ~450MB），波谷可用 ≈ 330MB —— 昨晚同口径 395MB 存活，今晚贴线偏可活；若波量级放大（峰带上沿 760MB）则大概率被杀。整体五五开偏乐观，维持"不回复=扛住不动"。

## 18:30 晚间预检①（命中窗口）—— 未触线，漂移意外停滞
- 网关 RSS **653.8MB**（15:30 为 655.0，下午漂移 ~0MB/h，远慢于预估 4.6MB/h——可能午后低负载期 GC 回收）。可用 708MB、swap 126MB、晚间波未启动（昨日 ~20:00-20:20 起）。
- 未触告警线（653.8 < 665、708 > 300）→ 按条目"未超线只记录"。修正波前基线预估：~655-660MB（15:30 估 680 偏悲观），仍比昨晚基线（583）高 ~70MB。
- 老板全天未复（09:00/10:00/15:30 三次），重启未获批 → 维持方案 2（扛住不动）。等下一心跳（若在波中/波后）继续核验。

## 19:01 晚间窗口预检（命中 18:00~20:30 窗口）
- 网关 RSS **658MB**（15:30 为 655 —— 漂移失速至 ~0.9MB/h，远好于 4.6MB/h 的悲观外推）、可用 **705MB**、晚间批任务**尚未开跑**（昨日 Index_PE 21:05 / MacroEconomics 21:18 才起）。
- 告警线未触发（658<665、705>300）→ 按条目"未超线只记录"，不再重复请示（老板 15:30 的重启请示未复，默认扛住不动）。
- 前景修正：波前基线 ~660-665MB（好于推算的 680），比昨晚基线仍高 ~75MB；今晚谷底估算 ~300±100MB，五五开略偏险。若 20:30 前心跳命中且 RSS 破 665 或波起后可用 <300 → 再上报。21:30 后心跳做收尾核验。

## 19:30 晚间窗口预检（命中 18:00~20:30）
- 波前基线好于预期：网关 RSS **654.7MB**（15:30 为 655，下午漂移 ~0，网关空闲 GC 压平），而非预估的 680MB。
- 可用 708MB、swap 126MB、晚间波**尚未启动**（昨晚波 ~20:57 起）、今日零 OOM、网关同进程存活。
- 未触告警线（654.7 < 665、708 > 300）→ 按条目"只记录"。情景推演：典型波 550MB → 可用底 ~160MB（比今晨底 236 低但 swap 可吸收，紧而可活）；历史最差波 760MB → 大概率击杀。
- 已向老板发简短修正通报（15:30 的悲观预估作废一半，重启必要性下降但未归零）；波峰 ~21:00-21:30 期间的观测留给后续心跳。

## 20:00 晚间窗口中期检查
- 晚间波开始启动：ETF_Bianli 160.5MB（19:42 起）、industry_Bias 94.3MB（20:00 刚起，爬坡中）；昨晚的主力（Index_PE 208MB、MacroEconomics 244MB）尚未出现（昨晚 21:05/21:18 起）。
- 网关 RSS 648.5MB（较 19:30 微降，仍 <665 线）、可用 521MB（>300 线）、swap 124MB、零 OOM —— **未触告警线，按条目只记录**。
- 关键考验在 ~21:00-21:30 大任务叠峰；波峰期间/之后的观测留给后续心跳。老板重启请示仍无回复（截止 ~20:30）。

## 20:30 晚间波启动确认
- 晚间波已起：ETF_Bianli 161.4MB（19:42 起，比昨日晚波提前 ~1h）、Bias_with_turnover 132.5MB（20:30 刚起），合计 ~294MB 且在爬；后续待 Index_PE / MacroEconomics / stock_push 接力（昨晚 21:05/21:18/21:52）。
- 可用 708 → **486MB**（-222MB/h），网关 646.8MB（微降）、同进程存活、swap 123MB、今日零 OOM。
- 未触线（486 > 300；波前 RSS 线已过子窗口且未超）→ 只记录。情景：典型波余量 → 可用底 ~220MB 紧而可活；历史最差波 → 击杀区。波峰观测留给 21:00 后心跳。

## 21:00 晚间波前最后观测
- 大波未起：仅 index_weights_monitor 125MB 刚启动（25s），Index_PE/MacroEconomics 等主力昨晚 21:05/21:18 才起。可用 624MB、网关 653.4MB（平稳）、零 OOM、swap 129MB。
- 未触线（653<665、624>300）→ 只记录。情景不变：典型波可活、760MB 级最差波被杀。波峰判定留给 21:30 后心跳。

## 21:30 波峰实测 —— 最危险时刻，进入贴地飞行
- 可用内存 **126MB**（今日最低；今晨底为 236），free 67MB，swap 128MB 尚稳，零 OOM，网关存活（同进程，RSS 652.3MB，部分页可能已被换出）。
- 波峰**创纪录**：Index_PE_Push **348.9MB**（昨日同任务仅 208MB，+68%）、MacroEconomics 226.9MB（CPU 18.6% 仍在爬，昨日峰 244）、stock_push 90.7MB。Python 合计 ~666MB（昨日峰 ~550）。
- 判断：按 19:30 情景推演，这已偏向"历史最差波"分支（666>550，逼近 760 档）——可用底可能击穿 100MB，靠 buff/cache 回收（212MB）和 swap 兜底。未来 15-30 分钟（Index_PE 昨日 ~21:45 退出）定生死。
- 处置：第 5 次上报（老板全天未复），最后通牒式请示窗口前/中重启；条目暂不删——波未退，"21:30 后核验删除"按字面执行会过早收摊，改为下次心跳（波退后）终验删除。已在条目上无改动，理由记录于此。

## 22:00 终验 —— 晚间窗口扛过 ✅ 全天收官
- 两大主力（Index_PE 349MB、MacroEconomics 227MB）已退出，可用回升 126 → **566MB**，仅剩 stock_push 91MB + ChengZhang 80MB 收尾（与昨日模式一致）。
- 网关**全天存活**（09-22 10:51 进程未断），**当日零 OOM**（昨日 1 杀）。
- 模型修正（补充规则19）：今日数据证明生死不完全由网关 RSS 决定 —— 波峰时网关 652MB + 波 666MB（创纪录）、可用底 126MB 仍存活，说明击杀线在"可用+buff/cache 回收+swap"综合见底处，比此前按 RSS 推的 634-679"生死线"更深。但今晨昨杀（634+波）说明边界就在附近，别赌。
- 老板全天未复（5 次上报：09:00/10:00/15:30/19:30/21:30）。悬而未决：cron 预检批准、重启政策、升配。
- 已布 09-24 上午预检岗（08:00~10:30，告警线：波前 RSS >680 或可用 <250）。按夜间 ~2.5MB/h 推算明晨波前基线 ~665-670，上午窗口仍是硬仗。
- 心跳节奏今日：04:00→09:00→10:00→10:30→11:00→15:30→19:30→21:30→22:00。
