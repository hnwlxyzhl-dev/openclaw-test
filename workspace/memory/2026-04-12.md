# 2026-04-12 Session Notes

## Bugs Fixed

### 1. Fundamentals 标注取值 bug（全部3个市场）
- **根因**：`yoy_match_count` / `qoq_match_count` 通过 `merge_asof(backward)` 合并到 `final`，会被 ffill 污染。标注取 `final.iloc[-1]` 拿到的是过期值（比如2020年的0），不是最新季度的真实匹配数。
- **Global_Index_PE.py（美股+港股）**：标注改为从 `strict_valid.iloc[-1]` 取 `latest_strict_yoy` / `latest_strict_qoq`，不再从 ffill 后的 `final` 取。同时 strict merge 不再传入 `yoy_match_count` 列。
- **Index_PE_Push.py（A股）**：标注改为从 `idx_q_plot.iloc[-1]` 取，不再从 `final_fund.iloc[-1]` 取。PEG 标注也用同一个 `yoy_n` 变量。

### 2. PB 注释硬编码 "Nasdaq"（Global_Index_PE.py ~line 1525）
- DJI 走同一个 US 代码路径，但注释硬编码了 "PB is NaN for Nasdaq"，DJI 图也错误显示 Nasdaq。
- 改为动态 `{index_name}`。

## Nasdaq 扩充 10→102 只

- 从 stockanalysis.com + nasdaq.com 获取完整 Nasdaq-100 成分股列表（101只证券，GOOGL+GOOG = 102个 ticker）
- 权重按市值比例分配，`normalize_weights` 自动归一化
- 验证了所有新增股票的 akshare `stock_financial_us_analysis_indicator_em(symbol, '单季报')` 都能拉到数据
- **50%门槛验证**：实际每季度覆盖98-100只，远超51只门槛，不会阻塞。只有早期（很多成分股未上市）和当前季度（财报季未结束）会被正确阻塞。

## 验证结论

- strict 聚合逻辑本身正确（验证了2025Q4港股21/21全部匹配）
- forward-fill 和 strict 两条路径的 50% 门槛都动态基于 `N_total`，无硬编码
- `N_total` 从10变102后，门槛从5变51，但数据覆盖率足够
- Detail table 仍然只展示前10大权重股（合理，避免 markdown 过长）
- `fetch_us_snapshot_market_cap` 遍历102只 × 0.3秒 = ~31秒，可接受
- akshare 拉取102只 × 2秒 = ~204秒，Nasdaq 单个指数约4分钟

## 待办（来自之前 session）

- [x] 修复 PB 注释硬编码 "Nasdaq" bug
- [x] 验证 50% 门槛在 102 只下是否影响程序
- [ ] Index_PE_Push.py A股尚未运行验证标注修复
