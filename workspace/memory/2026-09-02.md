# 2026-09-02

## 凌晨心跳（02:30）——遗留问题 #1 完整根因

### 事实链
1. 今晚 21:23 第四次 OOM（已预测），21:34 自动拉起，之后稳定
2. 23:00「GitHub配置备份」**实际成功**：写了 2 份备份（openclaw.json.bak.20260901_230005/230036），git 提交并推送 ✅——备份本身没问题
3. 今晚没有出现 cross-app 400 报错：因为完成通知这次**直接进了投递队列**（delivery-queue），还没被重试
4. **delivery-queue 积压 10 条消息**，全部是 08-26 以来的每晚备份完成通知，目标 `user:ou_4c8499b94572ae1598169c894bca1f83`，accountId `eden`，全部 400 失败（open_id cross app）

### 根因定论
Eden 飞书账号当前用的 app 与 `ou_4c8499...` 这个 open_id 的签发 app **不是同一个**（多半中途换过 app 凭据）。每晚通知都用这个失效 open_id 投递 → 必败 → 进队列无限重试。老板从 08-26 起就没收到过备份确认。

### 修复路径（等老板确认后执行）
- 方案A：老板在飞书给 Eden 发条消息 → 入站事件会带当前 app 的正确 open_id → 用它修复 cron announce 目标和队列消息的 to 字段
- 方案B：直接关闭该 cron 的完成通知（老板不需要每晚确认的话）
- 修复后清理 delivery-queue 里 10 条僵尸消息（避免无限重试）

### OOM 侧（不变，待拍板）
- 连续第 4 晚 21:23 被杀，自动恢复。方案：a) 观察泄漏 b) 内存上限兜底 c) 升配治本
- 老板全天未回复（09-01 的 4 条汇报均已送达 heartbeat 通道），明早如仍无回复，不再主动刷屏，等老板出现时一并汇报
