# 2026-07-18 工作记录

## 宏观经济推送程序(MacroEconomics_push.py)重大修复

### 1. GDP累计值Bug（已修复）
- **问题**：Tushare/AkShare/东财返回的GDP都是累计值（Q2=上半年累计），被当成单季值用
- **修复**：
  - 新增`fetch_gdp_from_statsbureau()`从统计局表2直接获取单季实际增速（0误差）
  - 新增`_cumulative_to_single_quarter()`拆分累计值为单季
  - 新增`_adjust_real_yoy_for_single_quarter()`推算单季实际增速
  - 新增`_compute_nominal_yoy()`计算名义增速
  - 图表新增红色名义GDP增速折线，标注最新季度
- **验证**：12个季度全部精确匹配官方值
- **备份**：`MacroEconomics_push.py.bak.20260718_120700`

### 2. PMI分项字段映射Bug（已修复）
- **问题**：Tushare的PMI字段代码映射全错
  - PMI010100 误认为"生产" → 实际是"大型企业PMI"
  - PMI010400 才是"生产"
  - 共11个分项映射全部错位
- **根因**：NBS统计局列表页翻页导致找不到PMI文章（7月15日集中发布大量文章挤到第2页），NBS覆盖失败后Tushare的错误映射暴露
- **修复**：通过官方值反推确认正确映射，全部校正

### 3. NBS翻页搜索（已修复）
- **新增通用函数** `find_nbs_article_url(keyword, max_pages=10, section='zxfb/sjjd')`
- 支持1-10页自动翻页，用StealthyFetcher优先，Fetcher降级
- 所有依赖统计局的函数已统一改造：
  - GDP `_find_latest_gdp_release_url` ✅
  - PMI `fetch_pmi_multi` ✅
  - PPI `fetch_ppi_multi` ✅
  - PMI分行业 `_fetch_pmi_cat_from_nbs` ✅
  - 工业利润 `_fetch_profit_from_nbs` ✅
  - CPI分项 `_fetch_cpi_article_text` ✅
  - 工增/社零/固投URL列表（已有25页翻页）保持不变

### 4. PMI出厂价格数据争议（已澄清）
- 4月出厂价格：Tushare=50.1（错），NBS=55.1（正确，统计局官方确认）
- 5月出厂价格：Tushare=48.5（错），NBS=51.9（正确）
- NBS数据覆盖Tushare是正确行为，NBS是权威数据源
- 程序当前输出完全正确

### 教训
- **不能用Tushare的值当"基准"验证NBS**，NBS才是权威
- 数据源字段定义可能在任何时候变更，需要定期交叉验证
- 统计局列表页文章会被新发布挤到后面，必须支持翻页
- PMI分项值都在47-54之间，标签错位很难肉眼发现，必须逐项验证官方值
