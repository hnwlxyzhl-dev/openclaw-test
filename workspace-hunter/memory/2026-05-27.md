# 2026-05-27 ETF基本面分析Skill开发

## 今日工作

### 1. ETF查询能力验证
- 查询了卫星ETF永赢159206行情和成分股（51只，2026Q1，akshare fund_portfolio_hold_em）
- 查询了通信ETF国泰515880行情

### 2. 创建OnDemand_Index_PE_Push.py
- 从 `/home/admin/Hualin/Index_PE_Push.py` 复制，原文件不动
- 修改点：
  - `process_index()` 新增 `external_stocks` 和 `is_etf` 参数
  - Step 1：有外部成分股时跳过 `index_weight`，直接用传入数据
  - Step 10：ETF模式跳过 `index_dailybasic`，直接汇总成分股市值
  - 价格数据：ETF用 `fund_daily` + akshare兜底，指数用 `index_daily`
  - `__main__` 改为命令行参数模式（`--name` `--code` `--stocks` `--is-etf`）
  - 计算逻辑完全没动

### 3. 创建skill: etf-index-fundamentals
- 位置：`/home/admin/.openclaw/workspace-hunter/skills/etf-index-fundamentals/`
- 已注册到 openclaw.json
- 文件：
  - `SKILL.md`：触发规则和工作流
  - `scripts/fetch_constituents.py`：获取成分股+权重，输出JSON
    - `--etf CODE`：ETF持仓（akshare fund_portfolio_hold_em，仅前10大）
    - `--etf CODE --index-code CSINDEX_CODE`：通过底层指数获取全部成分股（推荐）
    - `--csindex CODE`：直接查中证指数成分股
    - `--index CODE`：tushare指数权重

### 4. 实际运行结果
- **通信ETF国泰515880**：36只成分股（akshare），运行成功，推送3项内容到企业微信 ✅
- **卫星ETF易方达563530**：第一次只拿到10只（季报前10大），数据不对 ❌
  - 用户指出和同花顺不一致
  - 根因：563530跟踪"中证卫星产业指数"（931594），有50只成分股
  - akshare `fund_portfolio_hold_em` 只返回季报披露的前10大重仓股
  - 修复：更新 `fetch_constituents.py` 支持 `--index-code` 参数，通过CSIndex获取全部成分股
  - 第二次用50只成分股重跑，成功推送 ✅

### 5. 关键发现：ETF成分股获取策略
- **ETF季报（fund_portfolio_hold_em）**：只披露前10大重仓股，权重是占净值比例
- **底层指数（index_stock_cons_weight_csindex）**：返回全部成分股+权重，数据来自中证指数公司
- **结论：对ETF做基本面分析，必须通过底层指数获取成分股，不能只靠季报前10大**
- 需要知道ETF跟踪的指数代码（CSIndex格式，如931594）

### 6. 已知的ETF→指数映射
| ETF | 名称 | 底层指数 |
|-----|------|---------|
| 159206 | 卫星ETF永赢 | 待确认（可能是931594） |
| 563530 | 卫星ETF易方达 | 931594（中证卫星产业） |
| 515880 | 通信ETF国泰 | 待确认 |

## 待办 / 下次讨论
- [ ] 完善ETF→底层指数的自动查找逻辑（目前需要手动指定 --index-code）
- [ ] 通信ETF515880也需要用底层指数重跑？
- [ ] 卫星ETF永赢159206也需要确认底层指数并重跑
- [ ] 考虑在skill中加入自动ETF→指数映射表或自动查询功能
- [ ] SKILL.md中可能需要加入ETF→指数查找的详细步骤
